export const selectSpecialties = (state) =>
  state.specialty.specialties;

export const selectSpecialtyLoading = (state) =>
  state.specialty.loading;

export const selectSpecialtyError = (state) =>
  state.specialty.error;